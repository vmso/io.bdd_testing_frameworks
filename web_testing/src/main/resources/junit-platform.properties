# JUnit Platform Configuration for Parallel Execution
junit.jupiter.execution.parallel.enabled=true
junit.jupiter.execution.parallel.config.strategy=dynamic
junit.jupiter.execution.parallel.mode.default=concurrent
junit.jupiter.execution.parallel.mode.classes.default=concurrent

# Dynamic strategy configuration
junit.jupiter.execution.parallel.config.dynamic.factor=1.0
junit.jupiter.execution.parallel.config.dynamic.max-pool-size=8

# Test lifecycle configuration
junit.jupiter.testinstance.lifecycle.default=per_method

# Test discovery configuration
junit.jupiter.extensions.autodetection.enabled=true

# Test execution order
junit.jupiter.testmethod.order.default=org.junit.jupiter.api.MethodOrderer$OrderAnnotation
junit.jupiter.testclass.order.default=org.junit.jupiter.api.ClassOrderer$OrderAnnotation

# Test timeout configuration
junit.jupiter.execution.timeout.default=30s
junit.jupiter.execution.timeout.mode=abort

# Test reporting configuration
junit.jupiter.console.auto-detect=true
junit.jupiter.console.auto-detect.enabled=true

# Test filtering
junit.jupiter.extensions.autodetection.enabled=true