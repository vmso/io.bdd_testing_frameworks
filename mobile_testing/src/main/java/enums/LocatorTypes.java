package enums;

public enum LocatorTypes {
    ACCESSIBILITY_ID,
    CLASS_NAME,
    ID,
    NAME,
    XPATH,
    ANDROID_UI_AUTOMATOR,
    LINK_TEXT,
    PREDICATE_STRING,
    CLASS_CHAIN_IOS,
    TEXT
}
